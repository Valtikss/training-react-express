import AppBar from "@mui/material/AppBar";
import Grid from "@mui/material/Grid2";

/**
 * A functional component representing the top bar of the application.
 *
 * The top bar includes navigation buttons, a logo, and user information. It also provides
 * options for navigating to the settings page and logging out.
 *
 * @component
 * @returns {JSX.Element} The rendered top bar component.
 */
const Header = () => {
  return (
    <AppBar>
      <Grid container spacing={2}>
        {/* Icon */}
        <Grid>MiHIHIHI</Grid>
        {/* Commandes */}
        {/* Donnateurs */}
        {/* Recrutements */}
        {/* Mon compte */}
        {/* Plus */}
      </Grid>
    </AppBar>
  );
};

export default Header;
